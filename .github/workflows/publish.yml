name: 🚀 发布到 NPM

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 🔍 检测版本变化
        id: version
        run: |
          # 检测根目录 package.json 版本变化
          OLD=$(git show HEAD^:package.json 2>/dev/null | grep '"version"' | head -1 | sed 's/.*"\([^"]*\)".*/\1/' || echo "0.0.0")
          NEW=$(grep '"version"' package.json | head -1 | sed 's/.*"\([^"]*\)".*/\1/')
          echo "old=$OLD" >> $GITHUB_OUTPUT
          echo "new=$NEW" >> $GITHUB_OUTPUT
          if [ "$OLD" != "$NEW" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "📦 版本变化: $OLD → $NEW"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "⏭️ 版本未变化: $NEW"
          fi

      - name: ⚙️ 设置 pnpm
        if: steps.version.outputs.changed == 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: ⚙️ 设置 Node.js
        if: steps.version.outputs.changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          scope: "@mcp-decorator"
          cache: "pnpm"

      - name: 📦 安装依赖
        if: steps.version.outputs.changed == 'true'
        run: pnpm install --no-frozen-lockfile

      - name: 🔨 构建所有包
        if: steps.version.outputs.changed == 'true'
        run: pnpm run build

      - name: 🧪 运行测试
        if: steps.version.outputs.changed == 'true'
        run: pnpm test

      - name: 🚀 发布 Core 包
        if: steps.version.outputs.changed == 'true'
        run: |
          cd packages/core
          pnpm publish --access public --no-git-checks
          echo "✅ @mcp-decorator/core 发布成功"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: ⏳ 等待 NPM 索引更新
        if: steps.version.outputs.changed == 'true'
        run: |
          echo "等待 NPM 索引更新..."
          sleep 60

      - name: 🚀 发布 Math 插件
        if: steps.version.outputs.changed == 'true'
        run: |
          cd packages/plugin-math
          pnpm publish --access public --no-git-checks
          echo "✅ @mcp-decorator/plugin-math 发布成功"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: 🚀 发布 Filesystem 插件
        if: steps.version.outputs.changed == 'true'
        run: |
          cd packages/plugin-filesystem
          pnpm publish --access public --no-git-checks
          echo "✅ @mcp-decorator/plugin-filesystem 发布成功"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: 🚀 发布 HTTP 插件
        if: steps.version.outputs.changed == 'true'
        run: |
          cd packages/plugin-http
          pnpm publish --access public --no-git-checks
          echo "✅ @mcp-decorator/plugin-http 发布成功"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: 🏷️ 创建 Git 标签
        if: steps.version.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ steps.version.outputs.new }}" -m "Release v${{ steps.version.outputs.new }}"
          git push --tags

      - name: 📝 创建 GitHub Release
        if: steps.version.outputs.changed == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.new }}
          name: Release v${{ steps.version.outputs.new }}
          body: |
            ## 📦 发布的包

            - [@mcp-decorator/core@${{ steps.version.outputs.new }}](https://www.npmjs.com/package/@mcp-decorator/core)
            - [@mcp-decorator/plugin-math@${{ steps.version.outputs.new }}](https://www.npmjs.com/package/@mcp-decorator/plugin-math)
            - [@mcp-decorator/plugin-filesystem@${{ steps.version.outputs.new }}](https://www.npmjs.com/package/@mcp-decorator/plugin-filesystem)
            - [@mcp-decorator/plugin-http@${{ steps.version.outputs.new }}](https://www.npmjs.com/package/@mcp-decorator/plugin-http)

            ## 📚 文档

            - [快速入门](https://github.com/fromsko/mcp-decorator-framework/blob/main/docs/GETTING_STARTED.md)
            - [API 参考](https://github.com/fromsko/mcp-decorator-framework/blob/main/docs/API.md)
            - [插件开发](https://github.com/fromsko/mcp-decorator-framework/blob/main/docs/plugin-development.md)
            - [部署指南](https://github.com/fromsko/mcp-decorator-framework/blob/main/docs/deployment.md)

            ## 📝 变更日志

            查看 [CHANGELOG.md](https://github.com/fromsko/mcp-decorator-framework/blob/main/CHANGELOG.md) 了解详细变更。

            ## 🚀 安装

            ```bash
            npm install @mcp-decorator/core reflect-metadata zod
            ```
          draft: false
          prerelease: false
          generate_release_notes: true

      - name: ⏭️ 跳过发布
        if: steps.version.outputs.changed == 'false'
        run: echo "版本未变化，跳过发布"
